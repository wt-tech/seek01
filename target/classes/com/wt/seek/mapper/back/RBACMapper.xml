<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.wt.seek.dao.back.IRBACMapper">
<!--	List<Menu> listMenusByRoleId(@Param("roleId") Integer roleId);
	
	List<Menu> listAllMenus();-->
	
	<select id="listMenusByRoleId" resultType="Menu">
		SELECT
			menu.* 
		FROM
			role_menu
		LEFT JOIN menu ON role_menu.menu_id = menu.id
		WHERE
			role_menu.role_id = #{roleId};
	</select>
	
	
	<select id="listAllMenus" resultType="Menu">
		select * from menu;
	</select>
	
	<select id="listAllRoles" resultType="Role">
		select * from role;
	</select>
	<select id="listAllPermissions" resultType="Permission">
		select * from permission;
	</select>
	
	<update id="updatePermissions" parameterType="Permission">
	    update permission set description=#{description} where id=#{id}
	</update>
	
	<insert id="savePermissions" parameterType="Permission" 
	useGeneratedKeys="true" keyProperty="id">
	    insert into permission(url,description) values(#{url},#{description})
	</insert>
	
</mapper>